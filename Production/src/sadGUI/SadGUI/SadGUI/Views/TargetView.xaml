<UserControl x:Class="SadGUI.Views.TargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="25">
    <StackPanel Orientation="Horizontal">
        <TextBlock Width="160" Text="{Binding Name}" FontSize="18" />
        <TextBlock Width="75" Text="{Binding Friend}" FontSize="16" />
        <TextBlock Width="75" Text="{Binding Status}" FontSize="16" />
        <TextBlock Width="75" Text="{Binding Points}" FontSize="16" />
        <TextBlock Width="105" Text="{Binding FlashRate}" FontSize="16" />
        <TextBlock Width="75" Text="{Binding X}" FontSize="16" />
        <TextBlock Width="75" Text="{Binding Y}" FontSize="16" />
        <TextBlock Width="75" Text="{Binding Z}" FontSize="16" />
    </StackPanel>
</UserControl>
