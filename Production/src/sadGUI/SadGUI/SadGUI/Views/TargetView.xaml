<UserControl x:Class="SadGUI.Views.TargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="25">
    <StackPanel Orientation="Horizontal">
        <TextBlock Width="125" Text="{Binding Name}" />
        <TextBlock Width="75" Text="{Binding Friend}" />
        <TextBlock Width="75" Text="{Binding Status}" />
        <TextBlock Width="75" Text="{Binding Points}" />
        <TextBlock Width="75" Text="{Binding FlashRate}" />
        <TextBlock Width="75" Text="{Binding X}" />
        <TextBlock Width="75" Text="{Binding Y}" />
        <TextBlock Width="75" Text="{Binding Z}" />
    </StackPanel>
</UserControl>
